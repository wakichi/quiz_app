version: '3'

services:
  backend:
    build: ./backend/.
    command: python manage.py runserver 0.0.0.0:8000
    volumes: 
      - ./backend:/code/
    ports: 
      - "8000:8000"
    depends_on: 
      - db
  db:
    image: mysql
    environment:
      MYSQL_ROOT_PASSWORD: root
